/* pages/carpool/carpool.wxss */
.page { background: #FEFCFA; height: 100vh; display: flex; flex-direction: column; overflow: hidden; }

/* 分类Tab */
.cp-tabs {
  display: flex;
  padding: 0 32rpx;
  gap: 8rpx;
  background: #FEFCFA;
}
.cp-tab {
  padding: 24rpx 28rpx;
  font-size: 28rpx;
  color: #9A9A9A;
  position: relative;
}
.cp-tab.active {
  color: #2C2C2C;
  font-weight: 700;
}
.cp-tab.active::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 40rpx;
  height: 6rpx;
  background: #C67A52;
  border-radius: 3rpx;
}

.scroll-content { flex: 1; height: 0; }
.trip-list { padding: 16rpx 32rpx; }

.trip-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 32rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}

/* 类型标签 */
.card-type {
  display: inline-flex;
  align-items: center;
  gap: 6rpx;
  padding: 8rpx 18rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: 500;
  margin-bottom: 16rpx;
}
.type-offer { background: #F3E8DA; color: #C67A52; }
.type-seek { background: rgba(74, 144, 217, 0.08); color: #4A90D9; }

/* 用户信息 */
.card-header {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
}
.avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  margin-right: 16rpx;
  background: #FEFCFA;
}
.user-info { flex: 1; }
.user-name { font-size: 28rpx; font-weight: 600; color: #2C2C2C; display: block; }
.user-building { font-size: 22rpx; color: #9A9A9A; }
.time { font-size: 22rpx; color: #9A9A9A; }

/* 内容 */
.card-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #2C2C2C;
  display: block;
  margin-bottom: 12rpx;
  line-height: 1.4;
}
.card-desc {
  font-size: 26rpx;
  color: #9A9A9A;
  line-height: 1.7;
  display: block;
}

/* 路线信息 */
.route-section {
  margin-top: 20rpx;
  padding: 20rpx 24rpx;
  background: #FEFCFA;
  border-radius: 16rpx;
}
.route-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
}
.route-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
}
.dot-start { background: #C67A52; }
.dot-end { background: #4A90D9; }
.route-line {
  width: 2rpx;
  height: 24rpx;
  background: #ddd;
  margin-left: 7rpx;
  margin-top: 4rpx;
  margin-bottom: 4rpx;
}
.route-text {
  font-size: 26rpx;
  color: #2C2C2C;
  font-weight: 500;
}

/* 详细信息 */
.info-section {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-top: 16rpx;
}
.info-row {
  display: flex;
  align-items: center;
  gap: 6rpx;
}
.info-text { font-size: 24rpx; color: #9A9A9A; }
.fee {
  font-size: 26rpx;
  color: #C67A52;
  font-weight: 700;
}

/* 底部 */
.card-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 24rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #E3D5BF;
}
.participant-avatars {
  display: flex;
  align-items: center;
}
.p-avatar {
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  margin-right: -10rpx;
  border: 3rpx solid #fff;
  background: #FEFCFA;
}
.p-count {
  font-size: 22rpx;
  color: #9A9A9A;
  margin-left: 16rpx;
}

.card-actions { display: flex; gap: 12rpx; margin-left: auto; }
.btn-share, .btn-contact, .btn-join {
  width: 150rpx;
  height: 64rpx;
  line-height: 64rpx;
  font-size: 24rpx;
  font-weight: 600;
  border-radius: 16rpx;
  text-align: center;
  white-space: nowrap;
}
.btn-share {
  background: #FEFCFA;
  color: #9A9A9A;
  border: none;
}
.btn-contact {
  background: #FEFCFA;
  color: #2C2C2C;
  border: none;
}
.btn-join {
  background: #C67A52;
  color: #fff;
  border: none;
}
.btn-joined {
  background: #F3E8DA;
  color: #C67A52;
}
.btn-disabled {
  background: #E3D5BF;
  color: #9A9A9A;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 200rpx 0;
  gap: 20rpx;
}
.empty-text {
  font-size: 28rpx;
  color: #9A9A9A;
}

/* 浮动按钮 */
.fab-btn {
  position: fixed;
  right: 40rpx;
  bottom: 120rpx;
  width: 100rpx;
  height: 100rpx;
  background: linear-gradient(135deg, #D49A78 0%, #C67A52 100%);
  border-radius: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(198, 122, 82, 0.4);
}
.fab-btn:active { opacity: 0.85; }
