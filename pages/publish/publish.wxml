<!--pages/publish/publish.wxml-->
<view class="page">
  <!-- 自定义导航栏 -->
  <view class="custom-nav-wrap">
    <view style="height: {{statusBarHeight}}px;"></view>
    <view class="custom-nav-bar">
      <view class="nav-back" bindtap="goBack">
        <icon name="arrow-left" size="36" color="#1A1A1A"></icon>
      </view>
      <view class="nav-title">{{lockedType ? (publishType === 'carpool' ? '发布拼车' : publishType === 'pet' ? '发布宠物喂养' : publishType === 'sam' ? '发布山姆拼单' : publishType === 'help' ? '发布求帮忙' : publishType === 'post' ? '发布帖子' : publishType === 'free' ? '发布免费送' : publishType === 'activity' ? '发布活动' : '发布本地服务') : '发布'}}</view>
    </view>
  </view>

  <!-- 发布类型选择（从子页面进入时隐藏） -->
  <scroll-view scroll-x class="type-scroll" enhanced show-scrollbar="{{false}}" wx:if="{{!lockedType}}">
    <view class="type-tabs">
      <view class="type-tab {{publishType === 'product' ? 'active' : ''}}" bindtap="switchType" data-type="product">
        <icon name="package" size="28" color="{{publishType === 'product' ? '#fff' : '#C67A52'}}"></icon>
        <text>闲置物品</text>
      </view>
      <view class="type-tab {{publishType === 'activity' ? 'active' : ''}}" bindtap="switchType" data-type="activity">
        <icon name="calendar" size="28" color="{{publishType === 'activity' ? '#fff' : '#3CB371'}}"></icon>
        <text>发活动</text>
      </view>
      <view class="type-tab {{isLocalService ? 'active' : ''}}" bindtap="switchType" data-type="local">
        <icon name="map-pin" size="28" color="{{isLocalService ? '#fff' : '#5B9E6F'}}"></icon>
        <text>本地服务</text>
      </view>
      <view class="type-tab {{publishType === 'post' || publishType === 'help' ? 'active' : ''}}" bindtap="switchType" data-type="post">
        <icon name="message-circle" size="28" color="{{publishType === 'post' || publishType === 'help' ? '#fff' : '#4A90D9'}}"></icon>
        <text>发帖子</text>
      </view>
    </view>
  </scroll-view>

  <!-- 本地服务子类型选择 -->
  <view class="form-section" style="margin: 16rpx 32rpx 0;" wx:if="{{isLocalService && !lockedType}}">
    <view class="form-item" bindtap="showLocalSubPicker">
      <text class="form-label">服务类型</text>
      <view class="form-value">
        <text>{{publishType === 'pet' ? '宠物喂养' : publishType === 'sam' ? '山姆拼单' : '拼车顺路'}}</text>
        <icon name="caret-right" size="28" color="#ccc"></icon>
      </view>
    </view>
  </view>

  <scroll-view scroll-y class="form-scroll">

    <!-- 发布商品/免费表单（整体 wx:if 包裹，修复字段对所有类型可见的 bug） -->
    <view wx:if="{{publishType === 'product' || publishType === 'free'}}">

      <!-- 图片上传 -->
      <view class="form-section">
        <view class="upload-area">
          <view class="image-list">
            <view class="image-item" wx:for="{{imageList}}" wx:key="index">
              <image src="{{item}}" mode="aspectFill" class="uploaded-img"></image>
              <view class="remove-btn" bindtap="removeImage" data-index="{{index}}">
                <icon name="x" size="22" color="#fff"></icon>
              </view>
            </view>
            <view class="image-item add-btn" bindtap="chooseImage" wx:if="{{imageList.length < 9}}">
              <icon name="camera" size="48" color="#bbb"></icon>
              <text class="add-text">{{imageList.length}}/9</text>
            </view>
          </view>
        </view>
      </view>

      <!-- 标题 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">标题</text>
          <input class="form-input" placeholder="描述一下你的宝贝" maxlength="30" bindinput="onInput" data-field="title" value="{{form.title}}"/>
        </view>
      </view>

      <!-- 价格（非免费） -->
      <view class="form-section" wx:if="{{publishType === 'product'}}">
        <view class="form-item">
          <text class="form-label">价格</text>
          <input class="form-input" type="digit" placeholder="输入价格" bindinput="onInput" data-field="price" value="{{form.price}}"/>
          <text class="form-unit">元</text>
        </view>
        <view class="form-item">
          <text class="form-label">原价</text>
          <input class="form-input" type="digit" placeholder="可选" bindinput="onInput" data-field="originalPrice" value="{{form.originalPrice}}"/>
          <text class="form-unit">元</text>
        </view>
      </view>

      <!-- 成色 -->
      <view class="form-section">
        <view class="form-item" bindtap="showConditionPicker">
          <text class="form-label">成色</text>
          <view class="form-value">
            <text class="{{form.condition ? '' : 'placeholder'}}">{{form.condition || '选择成色'}}</text>
            <icon name="caret-right" size="28" color="#ccc"></icon>
          </view>
        </view>
      </view>

      <!-- 描述 -->
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">详细描述</text>
          <textarea class="form-textarea" placeholder="描述宝贝的使用状况、购买时间等" maxlength="500" bindinput="onInput" data-field="description" value="{{form.description}}"/>
        </view>
      </view>

      <!-- 交易方式 -->
      <view class="form-section">
        <view class="form-item" bindtap="showTradePicker">
          <text class="form-label">交易方式</text>
          <view class="form-value">
            <text class="{{form.tradeMethod ? '' : 'placeholder'}}">{{form.tradeMethod || '选择交易方式'}}</text>
            <icon name="caret-right" size="28" color="#ccc"></icon>
          </view>
        </view>
      </view>

    </view>

    <!-- 发帖表单 -->
    <view wx:if="{{publishType === 'post' || publishType === 'help'}}">
      <view class="form-section">
        <view class="form-item" bindtap="showPostCategoryPicker">
          <text class="form-label">帖子分类</text>
          <view class="form-value">
            <text class="{{form.postCategory ? '' : 'placeholder'}}">{{form.postCategory || '选择分类'}}</text>
            <icon name="caret-right" size="28" color="#ccc"></icon>
          </view>
        </view>
      </view>
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">标题</text>
          <input class="form-input" placeholder="{{form.postCategory === '求帮忙' ? '简短描述你需要的帮助' : '帖子标题'}}" maxlength="30" bindinput="onInput" data-field="title" value="{{form.title}}"/>
        </view>
      </view>
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">内容</text>
          <textarea class="form-textarea" placeholder="{{form.postCategory === '求帮忙' ? '详细描述你需要的帮助...' : '说点什么...'}}" maxlength="1000" bindinput="onInput" data-field="description" value="{{form.description}}" style="height: 300rpx;"/>
        </view>
      </view>
      <!-- 求帮忙：是否紧急 -->
      <view class="form-section" wx:if="{{form.postCategory === '求帮忙'}}">
        <view class="form-item">
          <text class="form-label">是否紧急</text>
          <switch checked="{{form.isUrgent}}" bindchange="onUrgentChange" color="#C67A52"/>
        </view>
      </view>
      <view class="form-section">
        <view class="upload-area">
          <view class="image-list">
            <view class="image-item" wx:for="{{imageList}}" wx:key="index">
              <image src="{{item}}" mode="aspectFill" class="uploaded-img"></image>
              <view class="remove-btn" bindtap="removeImage" data-index="{{index}}">
                <icon name="x" size="22" color="#fff"></icon>
              </view>
            </view>
            <view class="image-item add-btn" bindtap="chooseImage" wx:if="{{imageList.length < 9}}">
              <icon name="camera" size="48" color="#bbb"></icon>
              <text class="add-text">添加图片</text>
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- 山姆拼单表单 -->
    <view wx:if="{{publishType === 'sam'}}">
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">标题</text>
          <input class="form-input" placeholder="如：本周六山姆拼单" maxlength="30" bindinput="onInput" data-field="title" value="{{form.title}}"/>
        </view>
      </view>
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">详细描述</text>
          <textarea class="form-textarea" placeholder="描述拼单内容、采购计划等..." maxlength="500" bindinput="onInput" data-field="description" value="{{form.description}}"/>
        </view>
      </view>
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">截止时间</text>
          <view class="date-range-picker">
            <picker mode="date" bindchange="onInput" data-field="samDeadlineDate" value="{{form.samDeadlineDate}}" start="{{today}}">
              <text class="{{form.samDeadlineDate ? 'date-text' : 'placeholder'}}">{{form.samDeadlineDate || '选择日期'}}</text>
            </picker>
            <picker mode="time" bindchange="onInput" data-field="samDeadlineTime" value="{{form.samDeadlineTime}}">
              <text class="{{form.samDeadlineTime ? 'date-text' : 'placeholder'}}">{{form.samDeadlineTime || '选择时间'}}</text>
            </picker>
          </view>
        </view>
      </view>
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">取货方式</text>
          <input class="form-input" placeholder="如：送货上门、自提" maxlength="32" bindinput="onInput" data-field="samPickupMethod" value="{{form.samPickupMethod}}"/>
        </view>
      </view>
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">起拼金额</text>
          <input class="form-input" type="digit" placeholder="可选" bindinput="onInput" data-field="samMinAmount" value="{{form.samMinAmount}}"/>
          <text class="form-unit">元</text>
        </view>
        <view class="form-item">
          <text class="form-label">目标人数</text>
          <input class="form-input" type="number" placeholder="默认5人" bindinput="onInput" data-field="samTargetCount" value="{{form.samTargetCount}}"/>
          <text class="form-unit">人</text>
        </view>
      </view>
    </view>

    <!-- 宠物喂养表单 -->
    <view wx:if="{{publishType === 'pet'}}">
      <!-- 宠物发布类型 -->
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">发布类型</text>
          <view class="pet-type-selector">
            <view class="pet-type-option {{form.petPostType === 'need' ? 'active type-need' : ''}}" bindtap="onPetTypeChange" data-type="need">
              <icon name="paw-print" size="28" color="{{form.petPostType === 'need' ? '#E8883C' : '#888'}}"></icon>
              <text>寻求喂养</text>
            </view>
            <view class="pet-type-option {{form.petPostType === 'offer' ? 'active type-offer' : ''}}" bindtap="onPetTypeChange" data-type="offer">
              <icon name="heart" size="28" color="{{form.petPostType === 'offer' ? '#C67A52' : '#888'}}"></icon>
              <text>可以帮喂</text>
            </view>
          </view>
        </view>
      </view>

      <!-- 图片上传 -->
      <view class="form-section">
        <view class="upload-area">
          <view class="image-list">
            <view class="image-item" wx:for="{{imageList}}" wx:key="index">
              <image src="{{item}}" mode="aspectFill" class="uploaded-img"></image>
              <view class="remove-btn" bindtap="removeImage" data-index="{{index}}">
                <icon name="x" size="22" color="#fff"></icon>
              </view>
            </view>
            <view class="image-item add-btn" bindtap="chooseImage" wx:if="{{imageList.length < 9}}">
              <icon name="camera" size="48" color="#bbb"></icon>
              <text class="add-text">{{imageList.length}}/9</text>
            </view>
          </view>
        </view>
      </view>

      <!-- 标题 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">标题</text>
          <input class="form-input" placeholder="简短描述你的需求" maxlength="30" bindinput="onInput" data-field="title" value="{{form.title}}"/>
        </view>
      </view>

      <!-- 宠物名称 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">宠物名称</text>
          <input class="form-input" placeholder="可选，如：小花" maxlength="16" bindinput="onInput" data-field="petName" value="{{form.petName}}"/>
        </view>
      </view>

      <!-- 日期范围（寻求喂养） -->
      <view class="form-section" wx:if="{{form.petPostType === 'need'}}">
        <view class="form-item">
          <text class="form-label">日期范围</text>
          <view class="date-range-picker">
            <picker mode="date" bindchange="onPetStartDate" value="{{form.petStartDate}}" start="{{today}}">
              <text class="{{form.petStartDate ? 'date-text' : 'placeholder'}}">{{form.petStartDate || '开始日期'}}</text>
            </picker>
            <text class="date-separator">至</text>
            <picker mode="date" bindchange="onPetEndDate" value="{{form.petEndDate}}" start="{{form.petStartDate || today}}">
              <text class="{{form.petEndDate ? 'date-text' : 'placeholder'}}">{{form.petEndDate || '结束日期'}}</text>
            </picker>
          </view>
        </view>
      </view>

      <!-- 报酬（寻求喂养/可以帮喂） -->
      <view class="form-section" wx:if="{{form.petPostType === 'need' || form.petPostType === 'offer'}}">
        <view class="form-item">
          <text class="form-label">报酬</text>
          <input class="form-input" placeholder="如：200元/天 或 面议" maxlength="16" bindinput="onInput" data-field="reward" value="{{form.reward}}"/>
        </view>
      </view>

      <!-- 详细描述 -->
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">详细描述</text>
          <textarea class="form-textarea" placeholder="详细描述你的需求或服务..." maxlength="500" bindinput="onInput" data-field="description" value="{{form.description}}"/>
        </view>
      </view>
    </view>

    <!-- 拼车顺路表单 -->
    <view wx:if="{{publishType === 'carpool'}}">
      <!-- 拼车类型 -->
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">拼车类型</text>
          <view class="pet-type-selector">
            <view class="pet-type-option {{form.carpoolType === 'offer' ? 'active type-offer' : ''}}" bindtap="onCarpoolTypeChange" data-type="offer">
              <icon name="car" size="28" color="{{form.carpoolType === 'offer' ? '#C67A52' : '#888'}}"></icon>
              <text>有车带人</text>
            </view>
            <view class="pet-type-option {{form.carpoolType === 'seek' ? 'active type-need' : ''}}" bindtap="onCarpoolTypeChange" data-type="seek">
              <icon name="map-pin" size="28" color="{{form.carpoolType === 'seek' ? '#E8883C' : '#888'}}"></icon>
              <text>找车搭乘</text>
            </view>
          </view>
        </view>
      </view>

      <!-- 标题 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">标题</text>
          <input class="form-input" placeholder="简短描述拼车信息" maxlength="30" bindinput="onInput" data-field="title" value="{{form.title}}"/>
        </view>
      </view>

      <!-- 路线 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">出发地</text>
          <input class="form-input" placeholder="如：翡翠湾小区" maxlength="30" bindinput="onInput" data-field="carpoolFrom" value="{{form.carpoolFrom}}"/>
        </view>
        <view class="form-item">
          <text class="form-label">目的地</text>
          <input class="form-input" placeholder="如：南站高铁站" maxlength="30" bindinput="onInput" data-field="carpoolTo" value="{{form.carpoolTo}}"/>
        </view>
      </view>

      <!-- 日期时间 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">出发日期</text>
          <picker mode="date" bindchange="onInput" data-field="carpoolDate" value="{{form.carpoolDate}}">
            <view class="form-value">
              <text class="{{form.carpoolDate ? '' : 'placeholder'}}">{{form.carpoolDate || '选择日期'}}</text>
              <icon name="caret-right" size="28" color="#ccc"></icon>
            </view>
          </picker>
        </view>
        <view class="form-item">
          <text class="form-label">出发时间</text>
          <picker mode="time" bindchange="onInput" data-field="carpoolTime" value="{{form.carpoolTime}}">
            <view class="form-value">
              <text class="{{form.carpoolTime ? '' : 'placeholder'}}">{{form.carpoolTime || '选择时间'}}</text>
              <icon name="caret-right" size="28" color="#ccc"></icon>
            </view>
          </picker>
        </view>
      </view>

      <!-- 座位数 & 费用 -->
      <view class="form-section">
        <view class="form-item" wx:if="{{form.carpoolType === 'offer'}}">
          <text class="form-label">空余座位</text>
          <input class="form-input" type="number" placeholder="可搭乘人数" bindinput="onInput" data-field="carpoolSeats" value="{{form.carpoolSeats}}"/>
          <text class="form-unit">座</text>
        </view>
        <view class="form-item">
          <text class="form-label">费用</text>
          <input class="form-input" placeholder="如：每人¥20 或 免费" maxlength="20" bindinput="onInput" data-field="carpoolFee" value="{{form.carpoolFee}}"/>
        </view>
      </view>

      <!-- 详细描述 -->
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">详细描述</text>
          <textarea class="form-textarea" placeholder="描述路线、时间安排等详细信息..." maxlength="500" bindinput="onInput" data-field="description" value="{{form.description}}"/>
        </view>
      </view>
    </view>

    <!-- 活动表单 -->
    <view wx:if="{{publishType === 'activity'}}">
      <!-- 图片上传 -->
      <view class="form-section">
        <view class="upload-area">
          <view class="image-list">
            <view class="image-item" wx:for="{{imageList}}" wx:key="index">
              <image src="{{item}}" mode="aspectFill" class="uploaded-img"></image>
              <view class="remove-btn" bindtap="removeImage" data-index="{{index}}">
                <icon name="x" size="22" color="#fff"></icon>
              </view>
            </view>
            <view class="image-item add-btn" bindtap="chooseImage" wx:if="{{imageList.length < 9}}">
              <icon name="camera" size="48" color="#bbb"></icon>
              <text class="add-text">封面图</text>
            </view>
          </view>
        </view>
      </view>

      <!-- 标题 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">活动标题</text>
          <input class="form-input" placeholder="如：周末露营活动" maxlength="30" bindinput="onInput" data-field="title" value="{{form.title}}"/>
        </view>
      </view>

      <!-- 活动时间 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">开始时间</text>
          <view class="date-range-picker">
            <picker mode="date" bindchange="onInput" data-field="activityStartDate" value="{{form.activityStartDate}}" start="{{today}}">
              <text class="{{form.activityStartDate ? 'date-text' : 'placeholder'}}">{{form.activityStartDate || '选择日期'}}</text>
            </picker>
            <picker mode="time" bindchange="onInput" data-field="activityStartTime" value="{{form.activityStartTime}}">
              <text class="{{form.activityStartTime ? 'date-text' : 'placeholder'}}">{{form.activityStartTime || '选择时间'}}</text>
            </picker>
          </view>
        </view>
        <view class="form-item">
          <text class="form-label">结束时间</text>
          <view class="date-range-picker">
            <picker mode="date" bindchange="onInput" data-field="activityEndDate" value="{{form.activityEndDate}}" start="{{form.activityStartDate || today}}">
              <text class="{{form.activityEndDate ? 'date-text' : 'placeholder'}}">{{form.activityEndDate || '选择日期'}}</text>
            </picker>
            <picker mode="time" bindchange="onInput" data-field="activityEndTime" value="{{form.activityEndTime}}">
              <text class="{{form.activityEndTime ? 'date-text' : 'placeholder'}}">{{form.activityEndTime || '选择时间'}}</text>
            </picker>
          </view>
        </view>
      </view>

      <!-- 地点 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">活动地点</text>
          <input class="form-input" placeholder="如：小区中心花园" maxlength="60" bindinput="onInput" data-field="activityLocation" value="{{form.activityLocation}}"/>
        </view>
      </view>

      <!-- 费用 & 人数 -->
      <view class="form-section">
        <view class="form-item">
          <text class="form-label">活动费用</text>
          <input class="form-input" type="digit" placeholder="0 表示免费" bindinput="onInput" data-field="activityPrice" value="{{form.activityPrice}}"/>
          <text class="form-unit">元</text>
        </view>
        <view class="form-item">
          <text class="form-label">人数上限</text>
          <input class="form-input" type="number" placeholder="0 表示不限" bindinput="onInput" data-field="activityMaxParticipants" value="{{form.activityMaxParticipants}}"/>
          <text class="form-unit">人</text>
        </view>
      </view>

      <!-- 详细描述 -->
      <view class="form-section">
        <view class="form-item-col">
          <text class="form-label">活动详情</text>
          <textarea class="form-textarea" placeholder="描述活动内容、注意事项等..." maxlength="1000" bindinput="onInput" data-field="description" value="{{form.description}}" style="height: 300rpx;"/>
        </view>
      </view>
    </view>

    <view style="height: 260rpx;"></view>
  </scroll-view>

  <!-- 底部发布按钮 -->
  <view class="publish-bar safe-bottom">
    <button class="btn-publish" bindtap="onPublish">发布</button>
  </view>
</view>

<!-- 联系方式弹窗 -->
<view class="modal-mask" wx:if="{{showContactModal}}" bindtap="closeContactModal">
  <view class="contact-modal" catchtap="">
    <text class="contact-title">填写联系方式</text>
    <view class="contact-tip">方便其他人联系到你，至少填写一项</view>
    <view class="contact-form">
      <view class="contact-field">
        <text class="contact-label">手机号</text>
        <view class="contact-item">
          <input class="contact-input" type="number" maxlength="11" placeholder="输入手机号" value="{{contactPhone}}" bindinput="onContactPhone"/>
        </view>
      </view>
      <view class="contact-field">
        <text class="contact-label">微信号</text>
        <view class="contact-item">
          <input class="contact-input" placeholder="输入微信号" value="{{contactWechat}}" bindinput="onContactWechat"/>
        </view>
      </view>
    </view>
    <button class="contact-btn {{contactPhone || contactWechat ? '' : 'disabled'}}" bindtap="onContactConfirm">确认</button>
  </view>
</view>
